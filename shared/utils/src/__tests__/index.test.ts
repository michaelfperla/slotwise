describe('Utils Package', () => {
  describe('when importing the package', () => {
    it('should be importable', () => {
      // This is a placeholder test to ensure the test setup works
      expect(true).toBe(true);
    });
  });
});
