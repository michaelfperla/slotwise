{
  "name": "@slotwise/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for SlotWise platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "rimraf": "^6.0.1"
  },
  "files": [
    "dist/**/*"
  ]
}
